<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:padding="4dp"
    android:orientation="vertical">

  <!--date divider if needed-->
  <TextView
      android:id="@+id/tv_date_separator"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      tools:text="test"
      android:textColor="@color/primaryDarkColor"
      android:textStyle="bold"
      android:textSize="@dimen/event_text_size"
      android:layout_marginBottom="8dp"
      />

  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="horizontal"
      android:gravity="center_vertical"
      android:paddingStart="@dimen/event_text_padding"
      android:paddingEnd="@dimen/event_text_padding"
      >

    <ImageView
        android:id="@+id/iv_calendar_event_block"
        android:layout_width="@dimen/event_text_size"
        android:layout_height="@dimen/event_text_size"
        />

    <TextView
        android:id="@+id/tv_calendar_event_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/event_text_padding"
        android:paddingEnd="@dimen/event_text_padding"
        android:textSize="@dimen/event_text_size"
        />

    <TextView
        android:id="@+id/tv_calendar_event_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/event_text_padding"
        android:paddingEnd="@dimen/event_text_padding"
        android:textSize="@dimen/event_text_size"
        />

  </LinearLayout>


</LinearLayout>