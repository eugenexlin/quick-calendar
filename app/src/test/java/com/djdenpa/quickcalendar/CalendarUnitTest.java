package com.djdenpa.quickcalendar;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class CalendarUnitTest {
  @Test
  public void testCalendarTakesUTC() {
    // system uptime millis should be based on GMT
    // which is practically the same as UTC
    // confirmed so no longer need test
  }
}
